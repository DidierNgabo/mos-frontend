import ChangePasswordScreen from '@/app/screens/ChangePasswordScreen';

export const metadata = { title: 'Set New Password — MOS' };

export default function ChangePasswordPage() {
  return <ChangePasswordScreen />;
}
