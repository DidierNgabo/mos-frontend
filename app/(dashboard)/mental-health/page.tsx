import MentalHealthScreen from '@/app/screens/MentalHealthScreen';

export const metadata = { title: 'Mental Health — MOS' };

export default function MentalHealthPage() {
  return <MentalHealthScreen />;
}
